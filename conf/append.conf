# Dotfiles Sync - Append Mode Configuration
#
# Files listed here will be synced using APPEND mode:
#   - A managed block is inserted/updated between marker comments
#   - Content outside the markers is preserved
#   - Safe to run multiple times (idempotent)
#
# All other files tracked in user_home/ and root/ use OVERWRITE mode.
#
# Paths are relative to the repo root (e.g. user_home/.zshrc)

user_home/.zshrc
user_home/.bashrc
